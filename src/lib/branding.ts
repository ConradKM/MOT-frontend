// Central platform-brand constants. CoMaz OS is the *platform*; the
// individual garage/business a user is signed into (or booking with) remains
// the primary identity everywhere else in the UI — never replace a garage's
// own name with this. Import from here instead of hard-coding the platform
// name again.
export const PLATFORM_NAME = 'CoMaz OS'
export const PLATFORM_NAME_TM = 'CoMaz OS™'
export const PLATFORM_TAGLINE = `Powered by ${PLATFORM_NAME_TM}`
